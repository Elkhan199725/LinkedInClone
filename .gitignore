# ===================================
# Environment / Configuration
# ===================================

# Keep the base config in git
!appsettings.json

# Ignore dev/local variants (contain secrets)
appsettings.Development.json
appsettings.Local.json

# Optional: ignore any other local-only variants you use
appsettings.*.Local.json
appsettings.*.Development.json

# Environment files
.env
.env.*
*.env

# User secrets folder (rarely in repo, but safe)
.usersecrets/
